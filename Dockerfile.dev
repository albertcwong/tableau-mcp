FROM node:22-alpine
WORKDIR /app
RUN npm install -g tsx
COPY package.json package-lock.json ./
RUN npm ci --ignore-scripts
# src/, build/, mcp-app/ volume-mounted at runtime
COPY scripts/dev-docker.sh /app/scripts/
RUN chmod +x /app/scripts/dev-docker.sh
CMD ["/app/scripts/dev-docker.sh"]
